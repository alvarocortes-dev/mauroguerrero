import { describe, it, expect } from "vitest";

describe("AUTH-01: better-auth config", () => {
  it("TODO: auth instance initializes with drizzle adapter", () => {
    // Will import and test: import { auth } from "@/lib/auth/config"
    // expect(auth.api.getSession).toBeTypeOf("function")
    expect(true).toBe(false); // RED — implementation in Plan 02
  });
});
