import { describe, it, expect } from "vitest";

describe("AUTH-05: session management", () => {
  it("TODO: listDeviceSessions returns array of objects with id, userAgent, ipAddress", () => {
    // Will test: import { auth } from "@/lib/auth/config"
    // Call auth.api.listDeviceSessions with mock session context
    // Verify returned array has objects with { id, userAgent, ipAddress }
    expect(true).toBe(false); // RED — implementation in Plan 04
  });
});
